# Конструктор ростеров: оружие отдаётся только один раз

> **Статус:** закрыт
> **Итог:** в проде с v2.6.1 (2026-09-19, `0cd25f2`) — правило запаса + всё, что накопилось за день (Legends в конструкторе, GK в Империум, фазы «as if», карточка датащита, конструктор на телефоне без скролла страницы, импортёр по 1817 листам); хвосты → [paused/2026-09-13-player-report-tails.md](../../paused/2026-09-13-player-report-tails.md) §4–5
> **Репозитории:** wh11ed
> **Начато:** 2026-09-19
> **Связано:** [конструктор ростеров](../archive/2026/2026-08-03-roster-builder.md), [хвосты после жалоб](../paused/2026-09-13-player-report-tails.md)

## Жалоба

Игрок (скриншот, 2026-09-19): Chaos Lord with Jump Pack берёт plasma pistol (за bolt pistol),
power fist (за accursed weapon) и twin lightning claws (за оба) одновременно. «Это касается всех
отрядов. Нужно учитывать опциональное оружие и заменять его закачками; когда лимит кончается —
делать опции неактивными».

## Разбор

Данные знали всё: у каждой из трёх групп есть `rep` (что она заменяет; генератор читает это из
прозы, 918 из 1375 групп). Но единственным читателем `rep` была строка «starts equipped with»,
которая обрезала остаток в ноль и молчала. Взаимно гейтились (`cond`) только группы с явным «if
not equipped with X». Масштаб: 98 пар профиль/предмет на 72 листах отдавались дважды.

## Решение — правило запаса

Одно правило без новых данных: **на профиле предмет нельзя отдать больше раз, чем моделей его
несёт.** Где сделано и как устроено — `wh11ed/src/components/roster/CLAUDE.md`, раздел «The stock
rule». Коротко:

- `swapLedger` — единое место, где тратятся замены; `swapsByMini` (строка лоадаута, оверлей
  модификаторов, экспорт) не изменил смысла.
- Редактор: нетронутая группа, чьё заменяемое оружие кончилось, гаснет с причиной «Уже заменено
  другим выбором: bolt pistol» (`need: 'stock'`); степперы режутся до остатка. Группу с пиком не
  гасим — снять пик и есть способ починить.
- `validateRoster`: ошибка `overWargearReplaced` для сохранённых/импортированных списков; ничего
  не подрезаем автоматически.

Два решения, без которых ломались легальные списки:

1. **Пики суммируются по группе, потом режутся по моделям.** Devastator Sergeant, «two different
   weapons» за bolt pistol + boltgun — две строки одной группы на одной модели, одно разрешение.
2. **Предмет, который опция возвращает, не отдан.** Deathwatch Veterans: «boltgun and power
   weapon → power weapon and Astartes shield» оставляет power weapon, и сержант дальше меняет его
   на xenophase blade — так строит приложение GW и 13 списков корпуса. Поэтому у леджера две
   половины: `removed` (брутто, для показа) и `back` (возврат); запас — нетто. Комната спрашивается
   по опции, если опции возвращают разное.

Fail-open: `cp`-группы, `modelsPerMini` = null, предмет вне печатного лоадаута (цепочки, «X or Y»),
`total`-строки.

## Проверка

- vitest: 1868/1868 (новые: 7 кейсов в `rosterEngine.test.js`, 1 в `rosterValidation.test.js`).
- Корпус listhammer (658 списков, скачан заново `fetch-listhammer.py`): до/после отличается одной
  строкой — Deathwatch · Stephen Unger, сержант «power weapon отдан дважды». Это не правило, а
  импортёр: два одноимённых шилд-бандла с равным `fitOf`, берётся первый. Игрок увидит ошибку с
  именем оружия и переключит пункт. Стоковый tie-break в импортёре — отдельная работа, если
  повторится.
- lint, build — чисто. Chrome-смоук не делался (разрешения не спрашивал).

## Осталось

- Владельцу: посмотреть Лорда на джампе (все три комбинации) и Chaos Terminators (5 combi-weapon →
  тяжёлое гаснет) в редакторе.
- Релиз по слову владельца — вместе с Legends в конструкторе как **2.6.1** (`BUMP=none` (версия 2.6.1 руками)).
- Не сделано сознательно: importer tie-break по запасу; проверка Tempestus Aquilons (count 8 у
  lascarbine — «дефолты как итоги», в леджере ёмкость считается в моделях, так что не ломается).

## Что ещё едет в 2.6.1 (второй заход, 2026-09-19)

Вся сессия после стокового правила — тоже в незарелиженных коммитах wh11ed, ничего в проде:

- `d26c982` Grey Knights → группа Империума везде, где выбирают фракцию; группировка теперь
  из одного места (`factionsIndex.js`: `factionGroups` + `FACTION_GROUP_LABEL_KEYS`), трекер
  выводит свои группы из него, шесть копий карты подписей в компонентах убраны.
- `ef6815a` Разбор пяти баг-репортов (все удалены из очереди новым `feedback:delete`): «as if it
  were your X phase» больше не читается как срок — `stratagemPhases.js`, 26 записей индекса фаз;
  остальное — уже исправлено (Stealth в v2.4.3), не баг (Ufthak → таблица Legendary Proxies), не
  воспроизводится (Lieutenant с щитом: бандл-чекбокс есть и в редакторе, и в импорте).
- `48fc361` Пустой заголовок «Способности» над `abilities: []` (4 листа + 88 с пустым
  `wargearAbilities`) — секции карточки проверяют длину.
- `0773dcd` **Зазоры в карточке**: глобальное `table { margin: 1rem 0 }` из style.css сидело под
  таблицами оружия с первого коммита (замерено в Chrome: 32px между стрелковым и ближним). Плюс
  «⋯» вместо пяти кнопок в шапке на ≤480px контейнера (`.act-list`-шит), лор якорится к «⋯».
- `eab1520` Подписи булавки юнита: «Закрепить в списке юнитов» (`dsPinUnit`/`dsUnpinUnit`),
  фракционные `favPin` не тронуты.
- `60225d2` Название юнита 1.75rem на ≤480px / 1.6rem на ≤340px — промерено по самым длинным
  именам базы (54 симв.), строк не прибавляет на 360–430px.
- Чейнджлог 2.6.1 сжат до пяти заголовков по одной-две строки (`5225451`, `cc166c8`).

API: `ea75a2d` `npm run feedback:delete` — админ-скрипт, деплой api не нужен.

Следующий шаг без изменений: смоук владельца → релиз `BUMP=none` (версия 2.6.1 руками) (2.6.1).

## Третий заход (2026-09-19, вечер) — тоже в 2.6.1

- `cd3653a` пробел после названия способности (компилятор Vue режет перенос между элементами —
  4 блока карточки + печатная карточка).
- `edaf193` «Скрыто фильтрами: N».
- `9086929` + `eaab18f` **конструктор на телефоне не скроллит страницу**: корень экрана — flex-колонка
  в высоту окна (`.rw-host`/`.rw-fill` в style.css, замеры `--rw-top`/`--rw-below` в
  `RosterWorkbench`), панели берут остаток; первая версия мерила панели через ResizeObserver и
  дрожала на анимации аккордеона — заменена. Владелец выбрал этот вариант вместо вкладок
  «Юниты | Список». Шапка ужата, бар очков не переносится (порог 480px).
- `f6b13c5` шапка «Правила фракции» в тонированной плашке каталога.
- `31741d9` **импортёр против 1817 листов listhammer** (`fetch-listhammer.py --all` — API
  `/api/recentLists`): 9 правок, 148/157 текущих чисто (было 132); подробности и что осталось — в
  `src/components/roster/CLAUDE.md` («Checking the importer against real lists»).

**Открытые вопросы владельцу:**
- Vindicare 130 / Eversor 115 в двух GT-листах v946 против MFM v1.4 110/100 (сайт перескрейплен
  2026-09-19, не изменился) — обновление очков, которого нет на сайте? Не гуглить, спросить.
- Imperial Navy Breachers с мельтой и плазмой при одном las-volley: appdata лимит 1, приложение GW
  пропустило. Оставлено.

Следующий шаг: смоук владельца на телефоне (скролл конструктора, «⋯» в шапке датащита) → релиз
**2.6.1** обычным `npm run deploy` (автопатч 2.6.0 → 2.6.1, без BUMP).

## Четвёртый заход (2026-09-19, поздний вечер) — тоже в 2.6.1

- Чейнджлог: `ae2c967` «панель с очками всегда помещается в одну строку»; `4bf3789` + `b1cdd44`
  запись про импорт сжата («Я прогнал 1817 списков с listhammer — все читаются»). Владелец:
  чейнджлог пишется **от его лица в единственном числе**, не «мы/прогнали» → правила собраны в
  скиле `wh11ed/.claude/skills/changelog-entry` (строка в таблице скилов хаба `ca794ca`).
- `00e56b5` полоска `MobileUtilityBar` (чип «К партии») была на всю ширину без фона и съедала
  тап по «Прочие юниты» в конце каталога — `pointer-events: none` на полоске, `auto` на чипах;
  правая панель на ≤900px добавляет к концу прокрутки `--mobile-bar-h` (`8995883` — док).
  Предложено, не решено: прятать чип «К партии» на экранах конструктора вовсе (одна строка в
  `App.vue`).

Следующий шаг без изменений: смоук владельца на телефоне → релиз **2.6.1** обычным
`npm run deploy` (автопатч, без BUMP). wh11ed 23 незапушенных до `8995883`.

## Пятый заход (2026-09-19, ночь) — релиз

- `91f5568` фолд «Фильтры» в каталоге всегда стартует свёрнутым (решение владельца; в чейнджлог
  не пошло — владелец сказал «без неё»).
- `192ea3c` **пустой экран после «Сохранить» на dev-стенде** — регрессия `eaab18f`: комментарий
  перед корневым `<div>` мастера и редактора делал корень Fragment'ом в dev (комментарии там
  сохраняются, в проде вырезаются), `out-in` Transition в App.vue не получал конца leave и
  следующая вью не монтировалась. Воспроизведено в Chrome (с разрешения), корень `v-fgt` → `div`.
  Гейт: `vue/no-multiple-template-root` + `disallowComments` для `src/views/**` (eslint), заметка
  в `components/CLAUDE.md`. Прода не касалось.
- Релиз **v2.6.1** обычным `npm run deploy` (`0cd25f2`, деплой сам запушил все 27 коммитов);
  смоук: новый бандл `index-BXH1ebnd.js` с 2.6.1 на CDN и origin, страницы 200, CDN purged.
- Пост для группы ВК написан в чат по записи 2.6.1 (голос владельца); опубликован ли — не знаю.
